version: '2.4'
services:
  terminal:
    container_name: realworld-qpep-terminal
    #image: pavja2/qpep:terminal
    build: terminal/
    ports:
      - "3141"
      - "5358"
      - "5926"
    volumes:
      - ../../src/:/root/go/src
    environment:
      GOPATH: /root/go
      HTTP_PROXY: "http://cloud.jhuwyler.dev"
    networks:
      stlan:
        ipv4_address: 172.21.0.4
        priority: 1000
    privileged: true
    cap_add:
      - ALL
      - NET_ADMIN
    devices:
      - /dev/net/tun
    sysctls:
      - net.ipv6.conf.all.disable_ipv6=1
  ws-st:
    container_name: realworld-qpep-ws-st
    #image: pavja2/qpep:ws-st
    build: ws-st/
    networks:
      stlan:
        ipv4_address: 172.21.0.9
    privileged: true
    cap_add:
      - NET_ADMIN
    sysctls:
      - net.ipv6.conf.all.disable_ipv6=1
    environment:
      DISPLAY:
    volumes:
      - ../packet-captures/:/home/pcaps
  sitespeed:
    container_name: realworld-qpep-sitespeed
    build: sitespeed/
    cap_add:
      - NET_ADMIN
    sysctls:
      - net.ipv6.conf.all.disable_ipv6=1
    networks:
      stlan:
        ipv4_address: 172.21.0.15
networks:
  stlan:
    name: qpep-stlan
    driver: bridge
    enable_ipv6: true
    ipam:
      driver: default
      config:
        - subnet: 172.21.0.0/24
        - subnet: 2001:661:6602:142::1/64